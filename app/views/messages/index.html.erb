<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-10 col-lg-8">
      <div class="card shadow-sm">
        <div class="card-body">
          <h2 class="card-title mb-4 text-center">Messages reçus</h2>

          <% if @received_messages.any? %>
            <ul class="list-group mb-4">
              <% @received_messages.each do |msg| %>
                <li class="list-group-item">
                  <strong>De :</strong> <%= msg.sender.first_name %> <br>
                  <strong>Reçu le :</strong> <%= msg.created_at.strftime("%d/%m/%Y %H:%M") %> <br>
                  <p class="mb-0"><%= msg.content %></p>
                </li>
              <% end %>
            </ul>
          <% else %>
            <p class="text-center">Vous n'avez aucun message reçu.</p>
          <% end %>

          <div class="d-flex justify-content-center">
            <%= link_to "Messages envoyés", sent_messages_path, class: "btn btn-info me-2" %>
            <%= link_to "Nouveau message", new_message_path, class: "btn btn-potin" %>
          </div>
        </div> <!-- .card-body -->
      </div> <!-- .card -->
    </div> <!-- .col -->
  </div> <!-- .row -->
</div> <!-- .container -->
